rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow full read and write across all collections for local/admin operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
